<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/header') %>
    <title>Hola</title>
  </head>
  <body>
    <div class="container">
      <div class="row">

        <% books.forEach(function(book){ %>

          <div class="card">
            <div class="card-body">
              <p><%= book.title %></p>
            </div>
          </div>
        
         <% }); %>

        </div>
      </div>
    </div>
  </body>
</html>


